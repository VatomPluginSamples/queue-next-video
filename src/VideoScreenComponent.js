import { BaseComponent }  from 'vatom-spaces-plugins'


export class VideoScreenComponent extends BaseComponent {
//================ Properties of class VideoScreenComponent

  // Instance properties
  myCenter = null;


//================ Begin methods for class VideoScreenComponent

  // Called when the component is loaded
  async onLoad() {
  }

  // Called when the component is unloaded
  onUnload() {
  }// onUnload

  // Called when a remote message is received
  onMessage(msg) {
  }

  onObjectUpdated(newFields){
  }// onObjectUpdated()

//================ End of methods for class VideoScreenComponent
}// class VideoScreenComponent

